{
  "name": "sync-spike-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike Chrome extension: capture a narrated product session.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
