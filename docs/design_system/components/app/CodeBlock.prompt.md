A dark code surface with a Copy button — the embed snippet and other install code.

```jsx
<CodeBlock code={`<script src="https://cdn.flowbuddyai.com/copilot.js"\n        data-key="pk_live_8f2a…d41"></script>`} />
```

- The button writes to the clipboard and flips to "Copied" for ~1.4s. Pass `onCopy` to also fire a toast.
