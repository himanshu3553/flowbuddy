{
  "name": "@flowbuddy/widget",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "FlowBuddy embeddable copilot widget — one <script> renders an in-app chat grounded in approved-KB.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@flowbuddy/shared": "workspace:*",
    "html2canvas": "^1.4.1"
  }
}
