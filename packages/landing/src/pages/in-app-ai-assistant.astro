---
// Capability page — In-App AI Assistant. The hero is the homepage hero that
// shipped with the assistant-first positioning (headline, sub, and the looping
// invoice demo), reused verbatim here: it IS the assistant answering in
// context, which is this page's whole claim.
import Base from '../layouts/Base.astro';
import CapabilityHero from '../components/CapabilityHero.astro';
import CapabilitySteps from '../components/CapabilitySteps.astro';
import FeatureSplit from '../components/FeatureSplit.astro';
import PoweredByLayer from '../components/PoweredByLayer.astro';
import SignupCta from '../components/SignupCta.astro';
import PageFaq from '../components/PageFaq.astro';
import MockAssistant from '../components/MockAssistant.astro';
import type { Faq } from '../data/faqs';

const steps = [
  {
    title: 'Your user asks in place',
    body: 'A floating helper inside your product. Your user asks in their own words, on the page where they’re stuck — no help portal detour, no ticket raised.',
  },
  {
    title: 'It answers from approved knowledge only',
    body: 'Every answer is grounded in the workflows you recorded and approved, and shows its source. If something isn’t covered, it says "I don’t know yet" — and flags the gap for you.',
  },
  {
    title: 'It goes beyond words',
    body: 'When the answer is a task, the assistant doesn’t stop at describing it — it can highlight the next step on the page or guide the whole task, step by step.',
  },
];

const faqs: Faq[] = [
  {
    q: 'Will it ever make up an answer?',
    a: 'No. Every answer is grounded in workflows you recorded and approved, and each one shows its source. Anything not covered gets an honest "I don’t know yet" — plus a flag so you know what to record next.',
  },
  {
    q: 'How is this different from a chatbot trained on my docs?',
    a: 'It doesn’t read documents — it learned your product by watching you use it, so it knows the real steps, the real screens, and which page your user is on right now. And it answers only from what you approved, never from scraped content.',
  },
  {
    q: 'Does it change my product’s UI?',
    a: 'No. The assistant is a small floating overlay on top of your product. It never moves, resizes, or restyles anything on your pages, and you can match its accent color to your brand.',
  },
  {
    q: 'How fast can it go live?',
    a: 'About 30 minutes: record one product walkthrough, approve the workflows FlowBuddy derives from it, and paste one script tag into your app. No SDK, no code changes.',
  },
];

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

const breadcrumbJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://flowbuddyai.com/' },
    { '@type': 'ListItem', position: 2, name: 'In-App AI Assistant', item: 'https://flowbuddyai.com/in-app-ai-assistant' },
  ],
};

---

<Base
  title="In-App AI Assistant for your SaaS | FlowBuddy AI"
  description="Give your SaaS an in-app, context-aware AI assistant. Record your product once — your users get grounded answers inside your product, with sources shown and an honest 'I don't know yet'. Live in 30 minutes."
  path="/in-app-ai-assistant"
  jsonLd={[faqJsonLd, breadcrumbJsonLd]}
>
  <CapabilityHero
    ctaId="hero"
    sub="Record your product walkthrough once with our chrome extension and FlowBuddy automatically learns about your product to power an AI assistant for your users"
  >
    <Fragment slot="headline">
      In-App<br />Context-Aware<br />
      <span class="hero-ai">
        AI Assistant<svg viewBox="0 0 24 24" aria-hidden="true" class="hero-ai-spark">
          <defs>
            <linearGradient id="fb-sg-h1" x1="18%" y1="0%" x2="82%" y2="100%">
              <stop offset="0" stop-color="#4a63e8"></stop>
              <stop offset="1" stop-color="#3a50dd"></stop>
            </linearGradient>
          </defs>
          <path
            d="M10 3c.8 4.2 2.2 5.6 6.4 6.4-4.2.8-5.6 2.2-6.4 6.4-.8-4.2-2.2-5.6-6.4-6.4 4.2-.8 5.6-2.2 6.4-6.4Z"
            fill="url(#fb-sg-h1)"></path>
          <path
            d="M18.8 13.5c.5 2.4 1.2 3.2 3.7 3.7-2.4.5-3.2 1.2-3.7 3.7-.5-2.4-1.2-3.2-3.7-3.7 2.4-.5 3.2-1.2 3.7-3.7Z"
            fill="url(#fb-sg-h1)"></path>
        </svg>
      </span><br />for your SaaS
    </Fragment>

    <!-- HTML recreation: a host SaaS page with the assistant answering in context.
         Animated on load (typed question → streamed answer → nav spotlight);
         the markup below IS the end state, so no-JS / reduced-motion get it as-is. -->
    <div slot="demo" class="relative mx-auto w-full max-w-[560px]" aria-hidden="true" data-hero-demo>
      <div class="overflow-hidden rounded-dialog border border-gray-150 bg-white shadow-frame">
        <!-- browser chrome -->
        <div class="flex items-center gap-2 border-b border-gray-100 bg-paper-2 px-4 py-2.5">
          <span class="size-2.5 rounded-full bg-gray-200"></span>
          <span class="size-2.5 rounded-full bg-gray-200"></span>
          <span class="size-2.5 rounded-full bg-gray-200"></span>
          <span
            class="ml-3 rounded-pill bg-white px-3 py-1 font-mono text-[0.62rem] text-gray-400"
            >yoursaas.com/billing</span
          >
        </div>
        <div class="flex">
          <!-- host sidebar -->
          <div class="hidden w-[130px] shrink-0 space-y-1 border-r border-gray-100 bg-paper-2 p-3 sm:block">
            <div class="rounded-control px-2.5 py-1.5 text-[0.7rem] font-semibold text-gray-500">
              Dashboard
            </div>
            <div
              data-anim="invoices"
              class="relative rounded-control bg-brand-50 px-2.5 py-1.5 text-[0.7rem] font-bold text-brand-600 ring-2 ring-brand-400 ring-offset-1 transition-all duration-300"
            >
              Invoices
            </div>
            <div class="rounded-control px-2.5 py-1.5 text-[0.7rem] font-semibold text-gray-500">
              Team
            </div>
            <div class="rounded-control px-2.5 py-1.5 text-[0.7rem] font-semibold text-gray-500">
              Settings
            </div>
          </div>
          <!-- host content (striped placeholders) -->
          <div class="min-h-[290px] flex-1 space-y-3 p-4">
            <div class="media-fill h-6 w-2/5 rounded-control"></div>
            <div class="media-fill h-20 rounded-tile"></div>
            <div class="media-fill h-9 rounded-control"></div>
            <div class="media-fill h-9 w-4/5 rounded-control"></div>
          </div>
        </div>
      </div>

      <!-- the assistant panel -->
      <div
        class="absolute -bottom-6 -right-2 w-[280px] rounded-card border border-gray-100 bg-white shadow-widget sm:-right-6 sm:w-[300px]"
      >
        <div class="flex items-center gap-2 border-b border-gray-100 px-4 py-3">
          <span
            class="grid size-6 place-items-center rounded-[7px] text-[0.65rem] font-extrabold text-white"
            style="background: linear-gradient(150deg, #4a63e8, #3a50dd)"
            >F</span
          >
          <span class="text-[0.82rem] font-bold text-ink">Help</span>
          <span class="ml-auto flex items-center gap-1.5 text-[0.62rem] font-semibold text-success-text">
            <span class="size-1.5 rounded-full bg-success-dot"></span>Online
          </span>
        </div>
        <div class="space-y-3 px-4 py-4">
          <div
            data-anim="q"
            class="ml-auto w-fit max-w-[85%] rounded-tile bg-brand-500 px-3 py-2 text-[0.76rem] font-medium leading-snug text-white"
          >
            How do I download an invoice?
          </div>
          <div
            data-anim="a"
            class="w-fit max-w-[92%] rounded-tile border border-gray-100 bg-paper px-3 py-2.5 text-[0.76rem] leading-relaxed text-gray-700"
          >
            <span data-anim="dots" class="hidden items-center gap-1 py-1">
              <span class="fb-dot"></span><span class="fb-dot"></span><span class="fb-dot"></span>
            </span>
            You're close — invoices live one step away.
            <span class="mt-1.5 block font-semibold text-ink"
              >1. Open <span class="text-brand-600">Invoices</span> in the sidebar — I've
              highlighted it for you.</span
            >
            <span class="block font-semibold text-ink"
              >2. Choose an invoice and select Download PDF.</span
            >
            <span
              data-anim="chip"
              class="mt-2 inline-block rounded-pill border border-brand-100 bg-brand-50 px-2 py-0.5 font-mono text-[0.58rem] font-bold uppercase tracking-wide text-brand-600"
              >From: Download an invoice</span
            >
          </div>
          <button
            data-anim="walk"
            class="w-full rounded-control border border-brand-200 bg-brand-50 px-3 py-2 text-[0.74rem] font-bold text-brand-600"
            tabindex="-1">Walk me through it</button
          >
        </div>
      </div>
    </div>
  </CapabilityHero>

  <CapabilitySteps
    eyebrow="How it answers"
    heading="From question to unblocked, inside your product"
    steps={steps}
  />

  <FeatureSplit
    eyebrow="Grounded by design"
    heading="Answers your users can trust"
    lead="A generic chatbot answers from whatever it was fed. This assistant answers from your product — and only the parts you approved."
    bullets={[
      'Grounded in workflows you recorded and approved — nothing else',
      'Shows the source behind every answer',
      'Says "I don’t know yet" honestly, and flags the gap for you',
      'Knows which page your user is on when they ask',
      'A floating overlay that never touches your product’s layout',
    ]}
  >
    <MockAssistant />
  </FeatureSplit>

  <PoweredByLayer current="assistant" />
  <SignupCta />
  <PageFaq heading="Questions about the assistant" items={faqs} />
</Base>

<!-- Plays the demo once on load: typed question → typing dots → streamed answer →
     citation + walkthrough button → nav spotlight. Runs synchronously before first
     paint (no flash); any early return leaves the static end state untouched. -->
<script is:inline>
  (() => {
    const demo = document.querySelector('[data-hero-demo]');
    if (!demo || matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    const pick = (k) => demo.querySelector('[data-anim="' + k + '"]');
    const q = pick('q');
    const a = pick('a');
    const dots = pick('dots');
    const chip = pick('chip');
    const walk = pick('walk');
    const inv = pick('invoices');
    if (!q || !a || !dots || !chip || !walk || !inv) return;

    const qText = (q.textContent || '').trim();
    const nodes = [];
    const walker = document.createTreeWalker(a, NodeFilter.SHOW_TEXT);
    while (walker.nextNode()) {
      const n = walker.currentNode;
      if (chip.contains(n) || dots.contains(n) || !n.textContent) continue;
      nodes.push([n, n.textContent]);
    }

    const spotOn = ['bg-brand-50', 'text-brand-600', 'font-bold', 'ring-2', 'ring-brand-400', 'ring-offset-1'];
    const spotOff = ['text-gray-500', 'font-semibold'];

    // Freeze both bubbles at their final rendered size BEFORE emptying them, so
    // the panel stays fully open and nothing reflows while the text writes in.
    const lock = (el) => {
      const w = el.offsetWidth;
      const h = el.offsetHeight;
      el.style.width = w + 'px';
      el.style.height = h + 'px';
    };
    lock(q);
    lock(a);

    q.textContent = '';
    q.style.opacity = '0';
    q.style.transition = 'opacity 200ms';
    nodes.forEach(([n]) => (n.textContent = ''));
    a.style.visibility = 'hidden';
    chip.style.opacity = '0';
    chip.style.transition = 'opacity 300ms';
    walk.style.opacity = '0';
    walk.style.transition = 'opacity 300ms';
    inv.classList.remove(...spotOn);
    inv.classList.add(...spotOff);

    const sleep = (ms) => new Promise((r) => setTimeout(r, ms));
    const caret = document.createElement('span');
    caret.className = 'fb-caret';

    // The bubbles stay size-locked for the whole session (the panel width is a
    // fixed px class, so the frozen sizes never go stale) — the demo replays in
    // a loop, resetting to the empty state between runs.
    const reset = () => {
      q.textContent = '';
      q.style.opacity = '0';
      nodes.forEach(([n]) => (n.textContent = ''));
      a.style.visibility = 'hidden';
      chip.style.opacity = '0';
      walk.style.opacity = '0';
      inv.classList.remove(...spotOn, 'fb-pop');
      inv.classList.add(...spotOff);
    };

    (async () => {
      while (true) {
        await sleep(500);
        q.style.opacity = '1';
        q.appendChild(caret);
        for (const ch of qText) {
          caret.before(ch);
          await sleep(38);
        }
        await sleep(280);
        caret.remove();

        a.style.visibility = 'visible';
        dots.style.display = 'inline-flex';
        await sleep(800);
        dots.style.display = 'none';

        for (const [n, text] of nodes) {
          for (let i = 0; i < text.length; i += 2) {
            n.textContent = text.slice(0, i + 2);
            await sleep(14);
          }
          n.textContent = text;
        }

        await sleep(180);
        chip.style.opacity = '1';
        walk.style.opacity = '1';
        await sleep(380);
        inv.classList.remove(...spotOff);
        inv.classList.add(...spotOn, 'fb-pop');

        // Hold the finished state, then reset for the next run.
        await sleep(1500);
        reset();
      }
    })();
  })();
</script>
